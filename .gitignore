node_modules/
gitpush.sh~
/.babelrc~
*.blend1
.vscode/

# Test tooling output. tests/fixtures/*.blueprint3d ARE committed on purpose -
# they are the frozen migration baseline (regenerate with `npm run fixtures`).
coverage/
.vitest/

# Vite output. dist/ is the library bundle, dist-demo/ the built demo.
dist/
dist-demo/
tools/parity/

# legacy.html is GENERATED by `npm run legacy:index` from build/index.html - it
# is the pre-S6 jQuery demo, kept runnable beside the Vue app so the two can be
# compared during S6/S7. It goes when build/ does, in S9.
# (index.html at the repo root used to be generated the same way. As of S6 it is
# the Vue app's own entry, hand-written and committed.)
/legacy.html
